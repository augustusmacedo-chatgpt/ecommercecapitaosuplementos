export default async function handler(request: Request){const url=new URL(request.url);const cnpj=(url.searchParams.get('cnpj')||'').replace(/\D/g,'');if(cnpj.length!==14)return new Response(JSON.stringify({error:'CNPJ inválido'}),{status:400,headers:{'Content-Type':'application/json'}});try{const r=await fetch(`https://brasilapi.com.br/api/cnpj/v1/${cnpj}`);const text=await r.text();return new Response(text,{status:r.status,headers:{'Content-Type':'application/json'}})}catch{return new Response(JSON.stringify({error:'Consulta indisponível'}),{status:502,headers:{'Content-Type':'application/json'}})}}
