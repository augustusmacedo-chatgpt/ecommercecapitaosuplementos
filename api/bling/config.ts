import { json, readJsonBody, type BlingConfig } from '../../src/server/bling-shared.js';
import { hasPersistentStorage, loadStoredData, saveStoredData } from '../../src/server/bling-store.js';
export default async function handler(request: Request) { try { if (!hasPersistentStorage()) return json({ error: 'Armazenamento persistente do Bling ainda não está conectado na Vercel.' }, 503); const current = await loadStoredData(); if (request.method === 'GET') return json({ configured: Boolean(current?.clientId && current.clientSecret), clientId: current?.clientId || '', inviteLink: current?.inviteLink || '', secretConfigured: Boolean(current?.clientSecret) }, 200, { 'Cache-Control': 'no-store' }); if (request.method !== 'POST') return json({ error: 'Método não permitido.' }, 405); const body = await readJsonBody(request) as Partial<BlingConfig>; const clientId = body.clientId?.trim() || current?.clientId || ''; const clientSecret = body.clientSecret?.trim() || current?.clientSecret || ''; const inviteLink = body.inviteLink?.trim() || current?.inviteLink || ''; if (!clientId || !clientSecret) return json({ error: 'Client ID e Client Secret são obrigatórios.' }, 400); await saveStoredData({ clientId, clientSecret, inviteLink, refreshToken: current?.refreshToken }); return json({ ok: true, configured: true, clientId, secretConfigured: true, inviteLink }, 200, { 'Cache-Control': 'no-store' }); } catch (error) { console.error('Bling config error:', error); return json({ error: error instanceof Error ? error.message : 'Não foi possível salvar a configuração do Bling.' }, 500); } }
